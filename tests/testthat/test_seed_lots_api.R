# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test SeedLotsApi")

api.instance <- SeedLotsApi$new()

test_that("SeedlotsGet", {
  # tests for SeedlotsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get a filtered list of Seed Lot descriptions
  # Get a filtered list of Seed Lot descriptions available in a system.
  # @param seed.lot.db.id character Unique id for a seed lot on this server (optional)
  # @param germplasm.db.id character The internal id of the germplasm (optional)
  # @param external.reference.id character An external reference ID. Could be a simple string or a URI. (use with `externalReferenceSource` parameter) (optional)
  # @param external.reference.source character An identifier for the source system or database of an external reference (use with `externalReferenceID` parameter) (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [SeedLotListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SeedlotsPost", {
  # tests for SeedlotsPost
  # base path: https://test-server.brapi.org/brapi/v2
  # Add new Seed Lot descriptions to a server
  # Add new Seed Lot descriptions to a server
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param seed.lot.new.request array[SeedLotNewRequest]  (optional)
  # @return [SeedLotListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SeedlotsSeedLotDbIdGet", {
  # tests for SeedlotsSeedLotDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get a specific Seed Lot
  # Get a specific Seed Lot by seedLotDbId
  # @param seed.lot.db.id character Unique id for a seed lot on this server
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [SeedLotSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SeedlotsSeedLotDbIdPut", {
  # tests for SeedlotsSeedLotDbIdPut
  # base path: https://test-server.brapi.org/brapi/v2
  # Update an existing Seed Lot
  # Update an existing Seed Lot
  # @param seed.lot.db.id character Unique id for a seed lot on this server
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param seed.lot.new.request SeedLotNewRequest  (optional)
  # @return [SeedLotSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SeedlotsSeedLotDbIdTransactionsGet", {
  # tests for SeedlotsSeedLotDbIdTransactionsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get all Transactions related to a specific Seed Lot
  # Get all Transactions related to a specific Seed Lot
  # @param seed.lot.db.id character Unique id for a seed lot on this server
  # @param transaction.db.id character Unique id for a Transaction that has occurred (optional)
  # @param transaction.direction character Filter results to only include transactions directed to the specific Seed Lot (TO), away from the specific Seed Lot (FROM), or both (BOTH). The default value for this parameter is BOTH (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [SeedLotTransactionListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SeedlotsTransactionsGet", {
  # tests for SeedlotsTransactionsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get a filtered list of Seed Lot Transactions
  # Get a filtered list of Seed Lot Transactions
  # @param transaction.db.id character Unique id for a transaction on this server (optional)
  # @param seed.lot.db.id character Unique id for a seed lot on this server (optional)
  # @param germplasm.db.id character The internal id of the germplasm (optional)
  # @param external.reference.id character An external reference ID. Could be a simple string or a URI. (use with `externalReferenceSource` parameter) (optional)
  # @param external.reference.source character An identifier for the source system or database of an external reference (use with `externalReferenceID` parameter) (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [SeedLotTransactionListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SeedlotsTransactionsPost", {
  # tests for SeedlotsTransactionsPost
  # base path: https://test-server.brapi.org/brapi/v2
  # Add new Seed Lot Transaction to be recorded
  # Add new Seed Lot Transaction to be recorded
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param seed.lot.new.transaction.request array[SeedLotNewTransactionRequest]  (optional)
  # @return [SeedLotTransactionListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

