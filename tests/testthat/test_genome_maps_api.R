# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test GenomeMapsApi")

api.instance <- GenomeMapsApi$new()

test_that("MapsGet", {
  # tests for MapsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the Genomic Maps
  # Get list of maps
  # @param common.crop.name character The common name of the crop (optional)
  # @param map.db.id character The primary DbId for this genomic map (optional)
  # @param map.pui character The DOI or other permanent identifier for this genomic map (optional)
  # @param scientific.name character Full scientific binomial format name. This includes Genus, Species, and Sub-species (optional)
  # @param type character Type of map (optional)
  # @param program.db.id character Unique Id to filter by Program (optional)
  # @param trial.db.id character Unique Id to filter by Trial (optional)
  # @param study.db.id character Unique Id to filter by Study (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GenomeMapListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("MapsMapDbIdGet", {
  # tests for MapsMapDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the details of a specific Genomic Map
  # Provides the number of markers on each linkageGroup and the max position on the linkageGroup
  # @param map.db.id character The internal db id of a selected map
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GenomeMapSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("MapsMapDbIdLinkagegroupsGet", {
  # tests for MapsMapDbIdLinkagegroupsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the Linkage Groups of a specific Genomic Map
  # Get the Linkage Groups of a specific Genomic Map. A Linkage Group is the BrAPI generic term for a named section of a map. A Linkage Group can represent a Chromosome, Scaffold, or Linkage Group.
  # @param map.db.id character The internal db id of a selected map
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [LinkageGroupListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("MarkerpositionsGet", {
  # tests for MarkerpositionsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get marker position info
  # Get marker position information, based on Map, Linkage Group, and Marker ID
  # @param map.db.id character unique id of a map (optional)
  # @param linkage.group.name character The chromosome identifier or the generic linkage group identifier if the chromosome is not applicable. (optional)
  # @param variant.db.id character The unique id for a marker (optional)
  # @param min.position integer The minimum position (optional)
  # @param max.position integer The maximum position (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [MarkerPositionListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SearchMarkerpositionsPost", {
  # tests for SearchMarkerpositionsPost
  # base path: https://test-server.brapi.org/brapi/v2
  # Get marker position info
  # Get marker position information, based on Map, Linkage Group, and Marker ID
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param marker.position.search.request MarkerPositionSearchRequest  (optional)
  # @return [MarkerPositionListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SearchMarkerpositionsSearchResultsDbIdGet", {
  # tests for SearchMarkerpositionsSearchResultsDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get marker position info
  # Get marker position information, based on Map, Linkage Group, and Marker ID
  # @param search.results.db.id character Unique identifier which references the search results
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [MarkerPositionListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

