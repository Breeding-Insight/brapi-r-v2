# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test ReferencesApi")

api.instance <- ReferencesApi$new()

test_that("ReferencesGet", {
  # tests for ReferencesGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Gets a filtered list of &#x60;Reference&#x60; objects.
  # &#x60;GET /references&#x60; will return a filtered list of &#x60;Reference&#x60; JSON objects.
  # @param reference.db.id character The ID of the `Reference` to be retrieved. (optional)
  # @param reference.set.db.id character The ID of the `ReferenceSet` to be retrieved. (optional)
  # @param accession character If set, return the reference sets for which the `accession` matches this string (case-sensitive, exact match). (optional)
  # @param md5checksum character If specified, return the references for which the `md5checksum` matches this string (case-sensitive, exact match). (optional)
  # @param is.derived character If the reference is derived from a source sequence (optional)
  # @param min.length integer The minimum length of the reference sequences to be retrieved. (optional)
  # @param max.length integer The maximum length of the reference sequences to be retrieved. (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [ReferencesListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ReferencesReferenceDbIdBasesGet", {
  # tests for ReferencesReferenceDbIdBasesGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Lists &#x60;Reference&#x60; bases by ID and optional range.
  # Lists &#x60;Reference&#x60; bases by ID and optional range.
  # @param reference.db.id character The ID of the `Reference` to be retrieved.
  # @param start integer The start position (0-based) of this query. Defaults to 0. Genomic positions are non-negative integers less than reference length. Requests spanning the join of circular genomes are represented as two requests one on each side of the join (position 0). (optional)
  # @param end integer The end position (0-based, exclusive) of this query. Defaults to the length of this `Reference`. (optional)
  # @param page.token character The continuation token, which is used to page through large result sets. To get the next page of results, set this parameter to the value of `next_page_token` from the previous response. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [ReferenceBasesResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("ReferencesReferenceDbIdGet", {
  # tests for ReferencesReferenceDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Gets a &#x60;Reference&#x60; by ID.
  # &#x60;GET /references/{reference_id}&#x60; will return a JSON version of &#x60;Reference&#x60;.
  # @param reference.db.id character The ID of the `Reference` to be retrieved.
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [ReferenceSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SearchReferencesPost", {
  # tests for SearchReferencesPost
  # base path: https://test-server.brapi.org/brapi/v2
  # Gets a list of &#x60;Reference&#x60; matching the search criteria.
  # &#x60;POST /references/search&#x60; must accept a JSON version of &#x60;SearchReferencesRequest&#x60; as the post body and will return a JSON version of &#x60;SearchReferencesResponse&#x60;.
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param references.search.request ReferencesSearchRequest References Search request (optional)
  # @return [ReferencesListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SearchReferencesSearchResultsDbIdGet", {
  # tests for SearchReferencesSearchResultsDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Gets a list of &#x60;Reference&#x60; matching the search criteria.
  # &#x60;POST /references/search&#x60; must accept a JSON version of &#x60;SearchReferencesRequest&#x60; as the post body and will return a JSON version of &#x60;SearchReferencesResponse&#x60;.
  # @param search.results.db.id character Unique identifier which references the search results
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [ReferencesListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

