# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test GermplasmApi")

api.instance <- GermplasmApi$new()

test_that("BreedingmethodsBreedingMethodDbIdGet", {
  # tests for BreedingmethodsBreedingMethodDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the details of a specific Breeding Method
  # Get the details of a specific Breeding Method used to produce Germplasm
  # @param breeding.method.db.id character Internal database identifier for a breeding method
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [BreedingMethodSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("BreedingmethodsGet", {
  # tests for BreedingmethodsGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the Breeding Methods
  # Get the list of germplasm breeding methods available in a system.
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [BreedingMethodListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmGermplasmDbIdGet", {
  # tests for GermplasmGermplasmDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the details of a specific Germplasm
  # Germplasm Details by germplasmDbId was merged with Germplasm Multi Crop Passport Data. The MCPD fields are optional and marked with the prefix [MCPD].
  # @param germplasm.db.id character The internal id of the germplasm
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GermplasmSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmGermplasmDbIdMcpdGet", {
  # tests for GermplasmGermplasmDbIdMcpdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the details of a specific Germplasm in MCPD format
  # Get all MCPD details of a germplasm  &lt;a target&#x3D;\&quot;_blank\&quot; href&#x3D;\&quot;https://www.bioversityInternational.org/fileadmin/user_upload/online_library/publications/pdfs/FAOBIOVERSITY_MULTI-CROP_PASSPORT_DESCRIPTORS_V.2.1_2015_2020.pdf\&quot;&gt; MCPD v2.1 spec can be found here &lt;/a&gt;  Implementation Notes  - When the MCPD spec identifies a field which can have multiple values returned, the JSON response should be an array instead of a semi-colon separated string.
  # @param germplasm.db.id character the internal id of the germplasm
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GermplasmMCPDResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmGermplasmDbIdPedigreeGet", {
  # tests for GermplasmGermplasmDbIdPedigreeGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the pedigree details of a specific Germplasm
  # Get the parentage information of a specific Germplasm
  # @param germplasm.db.id character the internal id of the germplasm
  # @param notation character text representation of the pedigree (optional)
  # @param include.siblings character include array of siblings in response (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GermplasmPedigreeResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmGermplasmDbIdProgenyGet", {
  # tests for GermplasmGermplasmDbIdProgenyGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the progeny details of a specific Germplasm
  # Get the germplasmDbIds for all the Progeny of a particular germplasm.  Implementation Notes  - Regarding the &#39;&#39;parentType&#39;&#39; field in the progeny object. Given a germplasm A having a progeny B and C, &#39;&#39;parentType&#39;&#39; for progeny B refers to the &#39;&#39;parentType&#39;&#39; of A toward B.
  # @param germplasm.db.id character the internal id of the germplasm
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GermplasmProgenyResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmGermplasmDbIdPut", {
  # tests for GermplasmGermplasmDbIdPut
  # base path: https://test-server.brapi.org/brapi/v2
  # Update the details of an existing Germplasm
  # Germplasm Details by germplasmDbId was merged with Germplasm Multi Crop Passport Data. The MCPD fields are optional and marked with the prefix [MCPD].
  # @param germplasm.db.id character The internal id of the germplasm
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param germplasm.new.request GermplasmNewRequest  (optional)
  # @return [GermplasmSingleResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmGet", {
  # tests for GermplasmGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get a filtered list of Germplasm
  # Addresses these needs  - General germplasm search mechanism that accepts POST for complex queries   - Possibility to search germplasm by more parameters than those allowed by the existing germplasm search   - Possibility to get MCPD details by PUID rather than dbId
  # @param germplasm.pui character Permanent unique identifier (DOI, URI, etc.) (optional)
  # @param germplasm.db.id character Internal database identifier (optional)
  # @param germplasm.name character Name of the germplasm (optional)
  # @param common.crop.name character The common crop name related to this germplasm (optional)
  # @param accession.number character Unique identifiers for accessions within a genebank (optional)
  # @param collection character A specific panel/collection/population name this germplasm belongs to. (optional)
  # @param genus character Genus name to identify germplasm (optional)
  # @param species character Species name to identify germplasm (optional)
  # @param study.db.id character Search for Germplasm that are associated with a particular Study (optional)
  # @param synonym character Alternative name or ID used to reference this germplasm (optional)
  # @param parent.db.id character Search for Germplasm with this parent (optional)
  # @param progeny.db.id character Search for Germplasm with this child (optional)
  # @param external.reference.id character An external reference ID. Could be a simple string or a URI. (use with `externalReferenceSource` parameter) (optional)
  # @param external.reference.source character An identifier for the source system or database of an external reference (use with `externalReferenceID` parameter) (optional)
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GermplasmListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GermplasmPost", {
  # tests for GermplasmPost
  # base path: https://test-server.brapi.org/brapi/v2
  # Create new Germplasm entities on this server
  # Create new Germplasm entities on this server
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param germplasm.new.request array[GermplasmNewRequest]  (optional)
  # @return [GermplasmListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SearchGermplasmPost", {
  # tests for SearchGermplasmPost
  # base path: https://test-server.brapi.org/brapi/v2
  # Submit a search request for Germplasm
  # Search for a set of germplasm based on some criteria  Addresses these needs   - General germplasm search mechanism that accepts POST for complex queries   - Possibility to search germplasm by more parameters than those allowed by the existing germplasm search   - Possibility to get MCPD details by PUID rather than dbId  See Search Services for additional implementation details.
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @param germplasm.search.request GermplasmSearchRequest  (optional)
  # @return [GermplasmListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("SearchGermplasmSearchResultsDbIdGet", {
  # tests for SearchGermplasmSearchResultsDbIdGet
  # base path: https://test-server.brapi.org/brapi/v2
  # Get the results of a Germplasm search request
  # See Search Services for additional implementation details.  Addresses these needs:   1. General germplasm search mechanism that accepts POST for complex queries   2. possibility to search germplasm by more parameters than those allowed by the existing germplasm search   3. possibility to get MCPD details by PUID rather than dbId
  # @param search.results.db.id character Unique identifier which references the search results
  # @param page integer Used to request a specific page of data to be returned.  The page indexing starts at 0 (the first page is 'page'= 0). Default is `0`. (optional)
  # @param page.size integer The size of the pages to be returned. Default is `1000`. (optional)
  # @param authorization character HTTP HEADER - Token used for Authorization   <strong> Bearer {token_string} </strong> (optional)
  # @return [GermplasmListResponse]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

